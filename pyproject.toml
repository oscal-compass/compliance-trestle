# Added to aide with eventual transition away from setup.py
[build-system]
requires = [
    "setuptools >= 35.0.2<60.10.0",
    "setuptools_scm >= 2.0.0"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.2"
testpaths = [
    "tests",
]
