name: Conventional PR for develop
on:
  pull_request:
    branches:
      - develop

jobs:
  hqprs:
    runs-on: ubuntu-latest
    steps:
      - name: conventional-pr
        uses: Namchee/conventional-pr
        with:
          access_token: ${{ secrets.ADMIN_PAT }}
          check_draft: true
          link_issue: false
